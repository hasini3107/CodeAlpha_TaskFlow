PORT=5000
MONGODB_URI=
JWT_SECRET=
CLIENT_URL=http://localhost:5173
# Optional comma-separated DNS resolvers for MongoDB Atlas SRV lookup issues.
DNS_SERVERS=8.8.8.8,1.1.1.1

# Optional email delivery. Invitations still work in-app without these values.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
